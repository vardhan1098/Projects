import React from 'react';

const Count = (age,salary) => {
    return (
        <div>
            <h1>Age:{age}</h1>
            <h3>Salary:{salary}</h3>
        </div>
    );
};

export default Count;